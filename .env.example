# API 配置（OpenAI 兼容格式）
# 注意：现在使用页面配置，无需修改此文件
# 以下仅作为参考示例

# API 地址（末尾不要加 /chat/completions）
GEMINI_API_URL=https://api.example.com/v1

# API 密钥
GEMINI_API_KEY=sk-xxxxxxxxxx

# 文本分析模型（支持 vision）
GEMINI_MODEL_PRO=gemini-2.5-pro

# 图像生成模型（不支持时填 none）
GEMINI_MODEL_IMAGE=none
